syntax = "proto3";
service NotesService {
    rpc List (Empty) returns (NoteList) {}
}
message Empty {}

message NoteList {
  repeated Note notes = 1;
 }
message Note {
   string id = 1;
  string title = 2;
  string content = 3;
}

